import styled from 'styled-components';

export const Wrapper = styled.div`
  .products {
    display: block;
    width: 100%;
    padding: 0;
    margin: 2rem 0;

    .productsResults {
      margin: 0 -10px;
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: start;
    }
  }
`;
